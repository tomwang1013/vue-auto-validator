/*
 * define all validation methods
 */
module.exports = {
  required: function(depend) {
  },

  minLength: function(len) {
  },

  maxLength: function(len) {
  },

  min: function(v) {
  },

  max: function(v) {
  }
};
